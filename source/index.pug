
html
  head
    meta(name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no")


  body

    form(method="GET", action="https://www.dropbox.com/oauth2/authorize")
      input(type="hidden", name="client_id", value="irerc14heaj9q3e")
      input(type="hidden", name="response_type", value="token")
      input#redirect(type="hidden", name="redirect_uri", value="")
      input(type="submit", value="Get Token")


    script(type="module").
      import session from "./app/session.js";
      import config from "./app/config.js";

      document.getElementById('redirect').value = config.redirect;
      if (session.getSession()) {
        window.location.href = "/editor.html";
      }